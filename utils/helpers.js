// utils/helpers.js

module.exports = {
    capitalize: function(text) {
        return text.charAt(0).toUpperCase() + text.slice(1).toLowerCase();
    },
    formatDate: function(date, format) {
        // Implementación para formatear una fecha
    }
    // Puedes añadir más helpers aquí
};