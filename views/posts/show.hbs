<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{post.title}}</title>
</head>
<body>
  <h1>{{post.title}}</h1>
  <p>{{post.content}}</p>
  <p>Publicado: {{#if post.published}}Sí{{else}}No{{/if}}</p>
  <p>Creado en: {{post.createdAt}}</p>
  <p>Última actualización: {{post.updatedAt}}</p>

  <form action="/posts/{{post.id}}?_method=DELETE" method="POST">
    <button type="submit">Eliminar</button>
  </form>

  <a href="/posts">Volver a todos los posts</a>
</body>
</html>
